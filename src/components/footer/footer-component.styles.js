export default `
    <style>
    @import url('../common.css');

    .footer {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 6.25rem 5.375rem;
        background-color: var(--footer-color);
        color: var(--white);
    }
    
    </style>
`