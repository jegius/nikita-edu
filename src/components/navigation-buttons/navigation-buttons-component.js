import template from "./navigation-buttons-component.template.js";

export class NavigationButtonsComponent extends HTMLElement {
    static get name() {
        return "navigation-buttons-component";
    }

    constructor() {
        super();
        this.attachShadow({ mode: "open" });
    }

    connectedCallback() {
        this.#render();
    }

    #render() {
        const templateElem = document.createElement("template");
        templateElem.innerHTML = template;
        this.shadowRoot.appendChild(templateElem.content.cloneNode(true));
    }
}
