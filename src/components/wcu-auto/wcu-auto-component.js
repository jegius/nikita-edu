import template from "./wcu-auto-component.template.js";

export class WcuAutoComponent extends HTMLElement {
    static get name() {
        return "wcu-auto-component";
    }

    constructor() {
        super();
        this.attachShadow({ mode: "open" });
    }

    connectedCallback() {
        this.#render();
    }

    #render() {
        const templateElem = document.createElement("template");
        templateElem.innerHTML = template;
        this.shadowRoot.appendChild(templateElem.content.cloneNode(true));
    }
}
